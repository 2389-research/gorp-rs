# gorp - Environment Variables
#
# These environment variables override config.toml values.
# Copy this file to .env and fill in your values for Docker deployment.
#
# You can use EITHER environment variables OR config.toml (or both).
# Environment variables take precedence over config.toml values.

# === Matrix Settings (required) ===
MATRIX_HOME_SERVER=https://matrix.example.com
MATRIX_USER_ID=@bot:example.com
MATRIX_PASSWORD=your_password_here
# MATRIX_ACCESS_TOKEN=syt_...  # Alternative to password

# === Access Control (required) ===
# Comma-separated list of Matrix user IDs allowed to use the bot
ALLOWED_USERS=@you:example.com,@friend:example.com

# === Optional Matrix Settings ===
# MATRIX_DEVICE_NAME=gorp
# MATRIX_ROOM_PREFIX=Claude
# MATRIX_RECOVERY_KEY=EsTR mwqJ JoXZ 8dKN ...

# === Claude Settings ===
CLAUDE_BINARY_PATH=claude
# CLAUDE_SDK_URL=http://localhost:8080

# === Webhook Settings ===
# WEBHOOK_PORT=13000
# WEBHOOK_HOST=localhost
# WEBHOOK_API_KEY=your-secret-key

# === Workspace ===
# In Docker, use absolute paths:
# WORKSPACE_PATH=/home/gorp/workspace

# === Scheduler ===
# IANA timezone name (e.g., America/Chicago, Europe/London, UTC)
# SCHEDULER_TIMEZONE=America/Chicago

# === Logging ===
RUST_LOG=info
