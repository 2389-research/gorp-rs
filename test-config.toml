# Test Configuration for Encryption Testing
# Copy this to config.toml and fill in with test account credentials

[matrix]
home_server = "https://matrix.org"
user_id = "@harpertest1:matrix.org"
password = "ndg@VQA0ajr@drt5pqz"
device_name = "REPLACE_ME_WITH_UNIQUE_NAME"
allowed_users = ["@harpertest2:matrix.org"]
room_prefix = "TestBot"

[claude]
binary_path = "claude"

[webhook]
port = 13000
host = "localhost"

[workspace]
path = "./test-workspace"
