// ABOUTME: TUI view modules for each screen in the terminal interface
// ABOUTME: Each module renders its own content area and handles view-specific state

pub mod dashboard;
pub mod feed;
pub mod workspace;
